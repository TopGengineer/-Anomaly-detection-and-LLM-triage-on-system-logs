# Core
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
torch>=2.0

# Log parsing (Drain) — official logpai release of `logparser` on PyPI.
# NOT listed here because it pins regex==2022.3.2 (no wheels for py>=3.11);
# install it separately with:  pip install --no-deps logparser3
regex>=2023.0

# Hawkes process. WARNING: tick has no wheels for Python >= 3.10 and usually
# fails to build from source; see README for the scipy-based fallback plan.
# tick

# Dashboard + plotting
streamlit>=1.30
matplotlib>=3.7

# Utilities
requests>=2.31
tqdm>=4.66

# LLM triage — optional backend for the triage agent. The rule-based explainer
# runs without these; install anthropic (or ollama) to use a real model.
# anthropic
# ollama
