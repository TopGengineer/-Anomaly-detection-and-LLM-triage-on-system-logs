[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "logtriage"
version = "0.1.0"
description = "Log anomaly detection with Hawkes-process features and LLM triage"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
